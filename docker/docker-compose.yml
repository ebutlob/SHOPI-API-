version: '3.8'
services:
  shopi-api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8081:8081"
    volumes:
      - ../data:/app/data
    restart: unless-stopped
